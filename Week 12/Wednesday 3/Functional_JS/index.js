const a = 20
const b = 2
// Function is called, return value will end up in x

const part1 = function myFunction(a, b) {
    return a * b;             // Function returns the product of a and b
}

console.log(part1(4, 4));
console.log(part1(4, 2));
 // inputs need to be added to the function after the fact so that the logic can run
// function can be called on as many times as needed and also have diff values run through it
// logic will allways stay the same
